#include <cstring>
#include <iostream>
#include <string>

/* void operator+=(char *dst, const char src[]) {
    int i = std::strlen(dst);
    int j = 0;
    while (src[j] != '\0') {
        dst[i] = src[j];
        i++;
        j++;
    }
    dst[i] = '\0';
} */

int main() {
    std::cout << "ola" << std::endl;
    return 0;
}